{
  "$schema": "https://claude.com/schemas/hooks.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "(tool == \"Edit\" || tool == \"Write\") && (tool_input.file_path matches \"value-stream\" || tool_input.file_path matches \"vsm\" || tool_input.file_path matches \"value_stream_map\")",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/scripts/hooks/vsm-integrity.js",
            "timeout": 5000
          }
        ]
      }
    ]
  }
}
