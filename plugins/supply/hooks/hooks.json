{
  "$schema": "https://claude.com/schemas/hooks.json",
  "hooks": [
    {
      "matcher": "(tool == \"Edit\" || tool == \"Write\") && (tool_input.file_path matches \"supplier_map\" || tool_input.file_path matches \"supplier-map\" || tool_input.file_path matches \"sourcing\")",
      "hooks": [
        {
          "type": "command",
          "command": "node ${CLAUDE_PLUGIN_ROOT}/scripts/hooks/single-source-warning.js",
          "timeout": 5000
        }
      ]
    }
  ]
}
