{
  "$schema": "https://claude.com/schemas/hooks.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "(tool == \"Edit\" || tool == \"Write\") && (tool_input.file_path matches \"voc\" || tool_input.file_path matches \"customer-experience\" || tool_input.file_path matches \"voice_of\")",
        "hooks": [
          {
            "type": "command",
            "command": "node ${CLAUDE_PLUGIN_ROOT}/scripts/hooks/voc-quote-attribution.js",
            "timeout": 5000
          }
        ]
      }
    ]
  }
}
